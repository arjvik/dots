#!/bin/bash

sites=" ;voice.google.com;gmail.com;google.com;github.com;usamts.org;:open -p"

input="`echo "$sites"|rofi -dmenu -sep ';' -l 1 -columns 7 -p "qutebrowser"`"

if [[ "$input" != "" ]]; then
	if [[ "$input" != " " ]]; then
		qutebrowser --target window "$input"
	else
		qutebrowser --target window
	fi
fi
